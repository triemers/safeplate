// Review step shown after OCR extracts allergens from an uploaded photo/PDF
export default function AllergenReview({ extracted, onConfirm, onCancel }) {
  return (
    <section aria-label="Review extracted allergens">
      {/* TODO: implement after designs are ready */}
      <p>AllergenReview placeholder</p>
    </section>
  )
}
