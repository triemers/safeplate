// Shows pass/fail summary and flagged allergens after a scan
export default function ResultsDisplay({ results, allergens }) {
  return (
    <section aria-label="Scan results">
      {/* TODO: implement after designs are ready */}
      <p>ResultsDisplay placeholder</p>
    </section>
  )
}
